import { MatDialogConfig } from '@angular/material/dialog';
export const matDialogConfig: MatDialogConfig = {
    autoFocus: false,
    hasBackdrop: true,
    disableClose: false,
    restoreFocus: false,
};
